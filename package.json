{
  "name": "claude-codex-usage-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "A local Windows dashboard for Claude Code and Codex usage limits.",
  "main": "desktop/main.js",
  "scripts": {
    "start": "electron ./desktop/main.js",
    "start:desktop": "electron ./desktop/main.js",
    "postinstall": "node scripts/ensure-electron.js",
    "autostart:desktop": "install-desktop-autostart.bat",
    "autostart:desktop:off": "uninstall-desktop-autostart.bat",
    "setup:claude": "node setup-statusline.js",
    "setup:claude:fanout": "node setup-statusline.js --fanout",
    "test": "node --test",
    "check": "node --check server.js && node --check dashboard.js && node --check desktop/main.js && node --check desktop/preload.js && node --check setup-statusline.js && node --check statusline-usage.js && node --check statusline-both.js && node --check scripts/ensure-electron.js && node scripts/ensure-electron.js --check && npm test"
  },
  "keywords": [
    "claude",
    "codex",
    "usage-dashboard",
    "windows",
    "statusline"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "electron": "^43.1.0"
  }
}
